# Segmentation Module (Coming Soon)

This directory is reserved for pixel-wise classification tasks, such as Semantic Segmentation and Instance Segmentation. 

### Planned Features
- Full Convolutional Network (FCN) architecture.
- U-Net style encoder-decoder structures written natively in CUDA/cuDNN.
- Custom CUDA kernels for Intersection over Union (IoU) loss calculation.
- Optimized dataset loaders for image-mask pairs.
